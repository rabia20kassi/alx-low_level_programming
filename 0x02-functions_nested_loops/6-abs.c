#include "main.h"
/**
 * _abs - computes the absolute value of an int
 * @n: character being checked
 *
 * Return: abs of num
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
