#include "main.h"

/**
 * add - function that adds two integers and returns the result
 *@x: first value
 *@y: second value
 * Return: sum of two integers
 */

int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
